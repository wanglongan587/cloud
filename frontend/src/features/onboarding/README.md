# onboarding：创建或加入协作空间

## 职责

已登录用户无空间时展示创建与加入入口；有空间时进入已加入空间。创建操作建立新租户及其唯一空间。公网加入支持管理员分享的一次性邀请和需审批的申请链接，申请中的状态在空空间页面展示；华为内网提示联系管理员通过人员目录添加。

不负责认证、成员管理或跨源人员目录。

## 文件

| 文件 | 说明 |
| --- | --- |
| `onboarding-page.tsx` | 创建表单、链接输入、待审批状态和已有空间跳转 |
| `join-page.tsx` | 登录后恢复私有链接，并确认兑换邀请或提交申请 |
| `api.ts` | 邀请兑换、申请提交及我的申请列表 hooks |
| `*.test.tsx` | 页面和网络状态测试 |

## 依赖与不变量

依赖 `features/auth/session`、`features/spaces`、生成客户端、`src/lib/paths` 和 UI 组件；仅路由层消费页面。

- `/join/invite/:token` 和 `/join/apply/:token` 经过 `RequireSession`；未登录时令牌路径暂存在当前浏览器标签，Gateway 只收到无令牌的 `/join/continue` 返回路径，登录后再恢复链接。
- 邀请兑换只授予普通成员；申请提交后不能直接进入空间，必须等管理员审批。
- 链接输入只接受本站受支持的私有加入路径；创建 slug 在提交前通过与后端一致的校验。

## 测试

使用真实路由与 MSW 模拟创建、跳转、错误和待审批状态；不调用实际网关。
